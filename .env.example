DATABASE_URL="mysql://agaate:change-me@localhost:3306/agaate"
APP_SESSION_SECRET="generate-a-long-random-value-at-least-32-characters"
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_BUCKET="agaate-evidence"
S3_ACCESS_KEY_ID="change-me"
S3_SECRET_ACCESS_KEY="change-me"
S3_FORCE_PATH_STYLE="true"
WEATHER_PROVIDER_URL="https://api.open-meteo.com/v1/forecast"
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_RP_NAME="Agaate"
WEBAUTHN_ORIGIN="http://localhost:3000"
# 32 random bytes as base64 (generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
BIOMETRIC_ENCRYPTION_KEY="change-me-generate-32-bytes-base64"
# Cosine similarity threshold for face match — to be empirically validated in Stage 2 (do not use arbitrary default in production)
FACE_MATCH_THRESHOLD="0.6"
FACE_MODEL_ID="face-api-vladmandic"
FACE_MODEL_VERSION="0.2.0"
FACE_THRESHOLD_VERSION="2026-08-31-v1"
# Set to true to require Device+Face+Liveness for all FARM_OFFICER attendance (strict). False = transitional (only if enrolled).
REQUIRE_BIOMETRIC_FOR_ATTENDANCE="false"
